/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package TestPackage;

import java.util.ArrayList;
import java.util.Scanner;

/**
 *
 * @author harry
 */
public class NewClass1 {

    /*public static void main(String args[]) {
        Scanner scanner = new Scanner(System.in);
        System.out.print("Enter String: ");
        String test = scanner.nextLine();
        System.out.print("Enter group size: ");
        int size = scanner.nextInt();
        int length = test.length();
        boolean value = test.matches("[a-zA-Z0-9]+");

        if (value) {
            ArrayList<Character> chars = new ArrayList<Character>();

            int count = 0;
            for (int i = length - 1; i >= 0; i--) {
                if (count < size) {
                    chars.add(test.charAt(i));
                    count++;
                } else {
                    chars.add('-');
                    chars.add(test.charAt(i));
                    count = 0;
                }
            }

            for (int x = chars.size() - 1; x >= 0; x--) {
                System.out.print(chars.get(x));
            }
        }
        else{
            System.out.println("Entered string conatains invalid characters");
        }

    }*/

}
